{
  pkgs,
  nixpkgs-master,
  lib,
  system,
  systemType,
  baze,
  yas,
  tomorrowTheme,
  plenty,
  plz,
  edl-ng,
  claude-desktop,
  codex-desktop,
  llm-agents,
  zen-browser,
  kimi-code,
  trusted ? false,
  desktop ? false,
}:
[
  (import ./base.nix {
    inherit
      pkgs
      lib
      system
      systemType
      baze
      yas
      plenty
      zen-browser
      ;
  })
  (import ./desktop.nix {
    inherit
      pkgs
      lib
      system
      edl-ng
      claude-desktop
      codex-desktop
      llm-agents
      desktop
      ;
  })
  (import ./programs.nix {
    inherit
      pkgs
      lib
      nixpkgs-master
      system
      tomorrowTheme
      trusted
      kimi-code
      llm-agents
      plz
      ;
  })
]
